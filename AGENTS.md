# 在本仓库工作

先读根 README、当前项目 README。运行实验前再读 `docs/benchmark-methodology.md`；跨引擎比较还需读 `docs/engine-comparison.md`。配置字段和实现边界分别在 `docs/configuration.md`、`docs/architecture.md`。

- 用户当前目标和授权优先。只讨论或整理时不要启动实验；不要把一次测量请求扩大成全矩阵。已有授权范围内自主完成，不重复索要确认。
- 通用代码放 `src/`，项目配置放 `projects/<项目>/configs/`。先使用现有配置能力；只有明确阻塞才修公共代码并补必要测试，不把模型特例写进 runner。
- 运行前确认节点、GPU 空闲、固定镜像/权重、实际 CLI、日志准备和负载。没有得到跨节点任务授权，不访问其他机器。
- 复用正式 recipe 时保留原文件；优化用新 recipe/campaign。记录哪些内容已实测、哪些仅离线检查、哪些是候选。
- 不在实验运行期间修改源码或配置。保留 JIT 缓存；预热和测量独立保存，检查真实工作量。日志安静不等于性能收敛。
- 不放宽 gate 来取得 PASS。观察规则确实误判时，保存反例、修正精确识别并验证真实事件仍被捕获；保留 warning。
- 实验预算先明确：并发、请求量、重复数、预热及重试上限。出现 OOM、不支持或明显不稳定时取证，避免无限重试或只选最快结果。
- 只清理本次拥有的容器；不删模型、镜像、历史结果和缓存，不改变宿主功耗/锁频设置来掩盖差异。
- 精选结论放项目 `reports/`，小型逐次数据放 `data/`；原始结果放根 `results/`，本地草稿放根 `reports/`。文档使用相对链接，原始产物位置注明是否仅本机可用。
- 文档先写结论和适用范围，再写证据与复现入口。避免堆日志、重复规范或把候选配置称为官方最优。完成后检查语言、数字、单位和链接。
- 公共修复、配置变更、报告归档保持清楚的提交边界；未经要求不 push。修改后运行与变更有关的测试，纯目录迁移不重跑完整性能实验。
