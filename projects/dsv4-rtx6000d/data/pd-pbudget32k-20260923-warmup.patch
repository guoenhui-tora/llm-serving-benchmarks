--- a/projects/dsv4-rtx6000d/patches/mhc-startup-warmup-extended/dspark/warmup_plan.py
+++ b/projects/dsv4-rtx6000d/patches/mhc-startup-warmup-extended/dspark/warmup_plan.py
@@ -14,8 +14,8 @@
 
 
 def coverage(max_tokens, hidden, hc_mult, sms, captures=()):
-    if not (1 <= max_tokens <= 16384 and hidden == 4096 and hc_mult == 4 and sms > 0):
-        raise ValueError('This warmup is scoped to DSV4 Flash, max_tokens <= 16384')
+    if not (1 <= max_tokens <= 32768 and hidden == 4096 and hc_mult == 4 and sms > 0):
+        raise ValueError('This warmup is scoped to DSV4 Flash, max_tokens <= 32768')
     groups = {}
     for n in range(1, max_tokens + 1):
         groups.setdefault(signature(n, hidden, hc_mult, sms), []).append(n)
--- a/projects/dsv4-rtx6000d/patches/mhc-startup-warmup-extended/dspark/test_warmup_plan.py
+++ b/projects/dsv4-rtx6000d/patches/mhc-startup-warmup-extended/dspark/test_warmup_plan.py
@@ -16,15 +16,15 @@
             env = {'cache': cache, 'torch': SimpleNamespace(cuda=SimpleNamespace(get_device_properties=lambda _: SimpleNamespace(multi_processor_count=sms))),
                    'cdiv': lambda x,y: (x+y-1)//y}
             exec(compile(ast.Module(body=[node], type_ignores=[]), str(source), 'exec'), env)
-            sizes, groups = coverage(16384,4096,4,sms,(1,2,4,8,12,16,24,32))
+            sizes, groups = coverage(32768,4096,4,sms,(1,2,4,8,12,16,24,32))
             for hidden in (4096, 16384):
-                actual = {env['compute_num_split'](64,hidden,(n+63)//64) for n in range(1,16385)}
+                actual = {env['compute_num_split'](64,hidden,(n+63)//64) for n in range(1,32769)}
                 selected = {env['compute_num_split'](64,hidden,(n+63)//64) for n in sizes}
                 self.assertEqual(actual, selected)
-                for n in range(1,16385):
+                for n in range(1,32769):
                     self.assertEqual(split_for(n,hidden,sms),env['compute_num_split'](64,hidden,(n+63)//64))
             self.assertIn(1,sizes); self.assertIn(8,sizes); self.assertIn(16,sizes); self.assertIn(17,sizes)
-            self.assertIn(16384,sizes)
+            self.assertIn(32768,sizes)
 
     def test_draft_capacity_and_graph_bound(self):
         self.assertEqual(draft_query_bound(96, 5, [480, 576]), 576)
@@ -35,7 +35,7 @@
                 draft_query_bound(seqs, 5, captures)
 
     def test_rejects_out_of_scope_model_and_budget(self):
-        for params in [(16385,4096,4,156),(16384,16384,4,156),(16384,4096,8,156),(0,4096,4,156)]:
+        for params in [(32769,4096,4,156),(16384,16384,4,156),(16384,4096,8,156),(0,4096,4,156)]:
             with self.assertRaises(ValueError):coverage(*params)
 
 if __name__=='__main__': unittest.main()
--- a/projects/dsv4-rtx6000d/patches/input-kernel-warmup/test_input_warmup.py
+++ b/projects/dsv4-rtx6000d/patches/input-kernel-warmup/test_input_warmup.py
@@ -34,7 +34,7 @@
         geometries = [(512,512,64,64),(128,128,64,2),
                       (512,512,128,64),(128,256,128,2),(256,256,128,2)]
         drafts = [(16,8192,16384,5,5,0,64,256), (32,16384,32768,3,3,0,128,256),
-                  (96,16384,32768,5,5,0,128,256)]
+                  (96,16384,32768,5,5,0,128,256), (32,32768,32768,5,5,0,128,256)]
         record = run_coverage(geometries,drafts,device,budget_s=450)
         before = (cache_keys(topk),cache_keys(draft))
         # Replay different interior spans, arbitrary sliced mixed-token offsets,
--- a/projects/dsv4-rtx6000d/patches/input-kernel-warmup/input_warmup.py
+++ b/projects/dsv4-rtx6000d/patches/input-kernel-warmup/input_warmup.py
@@ -45,8 +45,8 @@
 
 
 def draft_spans(max_tokens, query_tokens):
-    if not 1 <= max_tokens <= 16384 or query_tokens not in (3, 5):
-        raise ValueError('Scope: DSV4 K3/K5, budget<=16384')
+    if not 1 <= max_tokens <= 32768 or query_tokens not in (3, 5):
+        raise ValueError('Scope: DSV4 K3/K5, budget<=32768')
     groups = {}
     for n in range(1, max_tokens + 1):
         block = min(256, 1 << (n + query_tokens - 1).bit_length())
@@ -94,7 +94,7 @@
     starts = [0]
     positions = []
     for length in lengths:
-        base = model_len - length - 1 if near_limit else 0
+        base = max(0, model_len - length - 1) if near_limit else 0
         positions += list(range(base, base + length))
         starts.append(starts[-1] + length)
     rejected = [int(span > 2)] + ([0] if n == 2 else [])
